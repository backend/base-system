{% extends 'base.twig' %}

{% block title %}Backend{% if title is not empty %} - {{  title }}{% endif %}{% endblock %}

{% block header %}
    <div class="container">
        <div class="page-header">
            <h1>{{ title }}</h1>
        </div>
    </div>
{% endblock %}

{% block body %}
    <div class="container">
        {% block content %}
            {{ content }}
        {% endblock %}
    </div>
{% endblock %}
